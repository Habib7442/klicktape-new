# ============================================================================
# KLICKTAPE SECURE ENVIRONMENT CONFIGURATION TEMPLATE
# ============================================================================
#
# Copy this file to .env and fill in your actual values
#
# SECURITY NOTICE:
# - EXPO_PUBLIC_ variables are embedded in your app bundle and visible to users
# - Only use EXPO_PUBLIC_ for non-sensitive, public configuration
# - Sensitive data (API keys, tokens) should NOT use EXPO_PUBLIC_ prefix
# - For production, set sensitive variables via EAS Environment Variables
#
# ============================================================================

# ============================================================================
# PUBLIC CLIENT-SIDE VARIABLES (Safe for EXPO_PUBLIC_)
# ============================================================================

# Supabase public configuration (safe to expose)
EXPO_PUBLIC_SUPABASE_URL=your_supabase_project_url
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Public service URLs (safe to expose)
EXPO_PUBLIC_SOCKET_SERVER_URL=http://192.168.38.201:3000
EXPO_PUBLIC_UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url

# Feature flags and non-sensitive configuration (safe to expose)
EXPO_PUBLIC_ENABLE_PERFORMANCE_MONITORING=true
EXPO_PUBLIC_DEBUG_MODE=false

# ============================================================================
# DEVELOPMENT ONLY: TEMPORARY EXPO_PUBLIC_ VERSIONS
# ============================================================================
# ⚠️ WARNING: These are ONLY for local development and should be removed for production
# 🚨 SECURITY RISK: These expose sensitive data in the client bundle
# 📝 TODO: Remove these and use EAS Environment Variables for production builds

# Development versions of secure variables (REMOVE FOR PRODUCTION)
EXPO_PUBLIC_UPSTASH_REDIS_REST_TOKEN=your_redis_token_here
EXPO_PUBLIC_GEMINI_API_KEY=your_gemini_api_key_here
EXPO_PUBLIC_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================================================
# SECURE SERVER-SIDE VARIABLES (NOT exposed to client)
# ============================================================================
#
# IMPORTANT: For production, set these via EAS Environment Variables:
#
# eas env:create --name UPSTASH_REDIS_REST_TOKEN --value "your_token" --environment production --visibility secret
# eas env:create --name GEMINI_API_KEY --value "your_key" --environment production --visibility secret
# eas env:create --name SUPABASE_SERVICE_ROLE_KEY --value "your_key" --environment production --visibility secret
#

# Redis authentication token (SENSITIVE - should be secret in production)
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token

# AI service API key (SENSITIVE - should be secret in production)
# Get from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key

# Database admin access (CRITICAL - should be secret in production)
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Server configuration
PORT=3000

# ============================================================================
# PRODUCTION DEPLOYMENT NOTES
# ============================================================================
#
# For production builds:
# 1. Remove sensitive variables from .env file
# 2. Set them via EAS Environment Variables with proper visibility:
#    - 'secret' for critical data (API keys, service keys)
#    - 'sensitive' for less critical but private data
#    - 'plain text' for non-sensitive configuration
# 3. Use different values for development/preview/production environments
# 4. Never commit .env files with real secrets to version control
#
# Alternative URLs for different environments:
# - Android Emulator: http://10.0.2.2:3000
# - iOS Simulator: http://localhost:3000
# - Physical Device: http://YOUR_COMPUTER_IP:3000
# - Production: https://your-deployed-server.com
#
# ============================================================================
